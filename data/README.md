# Data folder

This is just a placeholder for including data into the analyses. Generally, data either may not be shared explicitly, or it may be read directly from the web (via an API). Nevertheless, it is always a good idea to include a file like this explaining users how to access the data.
